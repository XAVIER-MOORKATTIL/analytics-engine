services:
  backend:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env

  frontend:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - backend

  data_engine:
    build: ./data_engine
    env_file:
      - .env
    depends_on:
      - backend